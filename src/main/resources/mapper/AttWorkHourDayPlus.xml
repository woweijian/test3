<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dongbao.test_demo3.mapper.AttWorkHourDayPlusMapper">

    <!-- 查询所有员工加班信息 -->
    <select id="selectOneByOriginId" resultType="com.dongbao.test_demo3.entity.AttWorkHourDay">
        SELECT * FROM att_work_hour_day where is_delete=0 and attendance_date=#{attendanceDate} and emp_origin_id=#{originId} and attendance_month=#{attendanceMonth}
    </select>



</mapper>